import React from 'react';

export const Categories = (): JSX.Element => (
    <div>This is the Categories page. This will show all of the categories
        This can be a side bar menu or its own page.
        Use the API to guide which categories should be included here.
    </div>
);